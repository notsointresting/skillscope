{
  "name": "cc-skillscope",
  "version": "0.3.0",
  "description": "Local-first CLI that reports which Claude Code skills, subagents and hooks actually fire, what they cost, and which are dead weight.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "skillscope": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "keywords": ["claude", "claude-code", "skills", "subagents", "hooks", "tokens", "cli"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
